<span>
    <form method="post" action="{{ path('cron_admin_cron_delete', {'id': cronJob.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
        <input type="hidden" name="_method" value="DELETE">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cronJob.id) }}">
        <button class="btn btn-danger">Delete</button>
    </form>
</span>
